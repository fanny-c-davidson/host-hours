# Supabase — same project as the web app. EXPO_PUBLIC_ vars are inlined into
# the client bundle (anon key is safe to ship; it's gated by RLS).
EXPO_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
EXPO_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
